/** StudentFlow | 학기 운영 보드: 오늘의 핵심 업무, 클릭 가능한 주간 일정, 조용한 보조 정보 */
import { ArrowRight, CalendarDays, ClipboardCheck, Megaphone, Plus, TriangleAlert } from "lucide-react";
import { useMemo, useState } from "react";
import { Link } from "wouter";
import { useApp } from "@/contexts/AppContext";
import { roles } from "@/lib/sample-data";
import { AppModal, Avatar, Button, StatusBadge } from "@/components/primitives";

const statusLabel = { TODO: "해야 할 일", IN_PROGRESS: "진행 중", IN_REVIEW: "검토 중", DONE: "완료", REJECTED: "반려" } as const;
const nameByRole: Record<string, string> = { MEMBER: "김서윤", DEPARTMENT_HEAD: "이도현", EXECUTIVE_BOARD: "박지후", TEACHER: "한미정" };
const dayMeta = [{ day: "금", date: "14", key: "2026-08-14" }, { day: "토", date: "15", key: "2026-08-15" }, { day: "일", date: "16", key: "2026-08-16" }, { day: "월", date: "17", key: "2026-08-17" }, { day: "화", date: "18", key: "2026-08-18" }, { day: "수", date: "19", key: "2026-08-19" }, { day: "목", date: "20", key: "2026-08-20" }];
type ScheduleType = "행사" | "업무" | "공지";
type ScheduleItem = { id: string; dateKey: string; type: ScheduleType; title: string; meta: string; href: string };
const typeStatus = { 행사: "예정", 업무: "해야 할 일", 공지: "모집 중" } as const;
const typeTone = { 행사: "bg-[#2563a8]", 업무: "bg-amber-500", 공지: "bg-slate-500" };

function WeekCalendar({ items, onSelect }: { items: ScheduleItem[]; onSelect: (key: string) => void }) {
  return <section className="mt-7 border-t-2 border-[#2563a8] bg-white"><div className="flex items-center justify-between gap-4 border-b border-slate-200 px-4 py-3 sm:px-5"><div className="flex items-center gap-2"><CalendarDays size={18} className="text-[#2563a8]"/><div><h2 className="font-bold">이번 주 일정</h2><p className="mt-0.5 text-xs text-slate-500">날짜를 누르면 그날의 일정을 볼 수 있어요.</p></div></div><Link href="/calendar" className="shrink-0 text-sm font-semibold text-[#2563a8] hover:underline">월간 보기</Link></div><div className="grid grid-cols-7">{dayMeta.map((item, index) => { const dayItems = items.filter((schedule) => schedule.dateKey === item.key); return <button key={item.key} onClick={() => onSelect(item.key)} className={`min-h-[72px] border-r border-slate-100 px-2 py-2.5 text-left hover:bg-[#f8fbfe] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-[#2563a8] sm:min-h-[84px] sm:px-3 ${index === 0 ? "bg-[#f7fbff]" : ""}`} aria-label={`${item.date}일 일정 ${dayItems.length}개 보기`}><div className="flex items-baseline justify-between gap-1"><span className={`text-xs font-bold ${index === 0 ? "text-[#2563a8]" : "text-slate-500"}`}>{item.day}</span><span className={`text-lg font-bold ${index === 0 ? "grid h-7 w-7 place-items-center rounded-full bg-[#2563a8] text-white" : "text-slate-700"}`}>{item.date}</span></div><div className="mt-2 flex items-center gap-1.5">{dayItems.slice(0, 3).map((schedule) => <i key={schedule.id} className={`h-1.5 w-1.5 rounded-full ${typeTone[schedule.type]}`}/>)}{dayItems.length > 0 && <span className="hidden text-[10px] font-semibold text-slate-500 sm:inline">{dayItems.length}개</span>}</div></button>; })}</div></section>;
}

function ReviewQueue() { const { submissions } = useApp(); const pending = submissions.filter((submission) => submission.status === "PENDING").slice(0, 2); if (!pending.length) return null; return <section className="mt-6 border-t-2 border-amber-500 bg-white"><div className="flex items-center justify-between border-b border-slate-200 px-4 py-3 sm:px-5"><div className="flex items-center gap-2"><TriangleAlert size={18} className="text-amber-700"/><h2 className="font-bold">검토 대기</h2></div><Link href="/submissions" className="text-sm font-semibold text-[#2563a8] hover:underline">검토하기</Link></div>{pending.map((submission) => <Link href="/submissions" key={submission.id} className="flex items-center gap-3 border-b border-slate-100 px-4 py-3 hover:bg-[#f8fbfe] sm:px-5"><Avatar label={submission.studentName} size="sm"/><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold text-slate-800">{submission.taskTitle}</p><p className="mt-1 text-xs text-slate-500">{submission.studentName} · {submission.submittedAt}</p></div><StatusBadge label="검토 대기"/></Link>)}</section>; }

export default function DashboardPage() {
  const { currentRole, currentUser, tasks, events, announcements } = useApp();
  const [selectedDate, setSelectedDate] = useState<string | null>(null);
  const scopedTasks = currentRole === "MEMBER" ? tasks.filter((task) => task.assigneeId === currentUser.id) : currentRole === "DEPARTMENT_HEAD" ? tasks.filter((task) => task.department === currentUser.department) : tasks;
  const urgentTasks = scopedTasks.filter((task) => ["TODO", "IN_PROGRESS", "REJECTED"].includes(task.status)).slice(0, 3);
  const scheduleItems = useMemo<ScheduleItem[]>(() => [
    ...tasks.filter((task) => task.status !== "DONE").map((task) => ({ id: `task-${task.id}`, dateKey: task.dueDate.slice(0, 10), type: "업무" as const, title: task.title, meta: `마감 ${task.dueDate.slice(11)} · ${task.department}`, href: `/tasks/${task.id}` })),
    ...events.map((event) => { const match = event.date.match(/(\d+)월\s*(\d+)일/); return { id: `event-${event.id}`, dateKey: `2026-08-${String(Number(match?.[2] ?? 1)).padStart(2, "0")}`, type: "행사" as const, title: event.title, meta: `${event.time} · ${event.location}`, href: `/events/${event.id}` }; }),
    ...announcements.filter((announcement) => announcement.pinned || announcement.application).map((announcement) => ({ id: `announcement-${announcement.id}`, dateKey: announcement.id === "a1" ? "2026-08-14" : "2026-08-17", type: "공지" as const, title: announcement.title.replace("[중요] ", ""), meta: announcement.application ? `신청 마감 ${announcement.application.deadline}` : `${announcement.target} 대상`, href: `/announcements/${announcement.id}` })),
  ], [tasks, events, announcements]);
  const selectedItems = selectedDate ? scheduleItems.filter((item) => item.dateKey === selectedDate) : [];
  const nextEvent = events[0]; const importantAnnouncement = announcements.find((announcement) => announcement.pinned) ?? announcements[0];
  return <div className="mx-auto max-w-[1120px] px-4 py-7 sm:px-6 lg:px-8"><header className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm text-slate-500">2026년 8월 14일 금요일</p><h1 className="mt-1 text-2xl font-bold tracking-[-.03em] text-slate-900">{nameByRole[currentRole]}님, 오늘 확인할 일</h1><p className="mt-2 text-sm text-slate-500">{roles[currentRole]} · {currentUser.department}</p></div><Link href="/tasks"><Button variant="secondary"><Plus size={17}/>업무 전체 보기</Button></Link></header><section className="border-y border-slate-200 bg-white"><div className="flex items-center justify-between gap-4 border-b border-slate-200 px-4 py-3 sm:px-5"><div className="flex items-center gap-2"><ClipboardCheck size={18} className="text-[#2563a8]"/><h2 className="font-bold">오늘 처리할 업무</h2></div><Link href="/tasks" className="text-sm font-semibold text-[#2563a8] hover:underline">업무로 이동</Link></div><div className="divide-y divide-slate-100">{urgentTasks.map((task) => <Link href={`/tasks/${task.id}`} key={task.id} className="group flex items-center gap-3 px-4 py-4 hover:bg-[#f8fbfe] sm:px-5"><div className="min-w-0 flex-1"><p className="font-semibold text-slate-800 group-hover:text-[#1f528b]">{task.title}</p><p className="mt-1 text-sm text-slate-500">마감 {task.dueDate}{task.status === "REJECTED" ? " · 수정 후 다시 제출" : ""}</p></div><StatusBadge label={statusLabel[task.status]}/><ArrowRight size={17} className="hidden text-slate-400 sm:block"/></Link>)}</div></section><WeekCalendar items={scheduleItems} onSelect={setSelectedDate}/><div className="mt-6 grid gap-4 lg:grid-cols-2"><section className="border-t-2 border-slate-700 bg-white"><div className="flex items-center justify-between border-b border-slate-200 px-4 py-3"><h2 className="font-bold">다음 행사</h2><Link href="/events" className="text-sm font-semibold text-[#2563a8] hover:underline">행사 보기</Link></div>{nextEvent && <Link href="/events" className="flex items-center gap-3 px-4 py-4 hover:bg-[#f8fbfe]"><div className="grid h-10 w-10 shrink-0 place-items-center border border-slate-200 bg-slate-50 text-center"><b className="text-xs text-[#2563a8]">{nextEvent.date.split(" ")[0]}</b><span className="-mt-2 text-[10px] text-slate-500">{nextEvent.date.split(" ")[1]}</span></div><div className="min-w-0"><p className="font-semibold text-slate-800">{nextEvent.title}</p><p className="mt-1 text-sm text-slate-500">{nextEvent.time} · {nextEvent.location}</p></div></Link>}</section><section className="border-t-2 border-[#2563a8] bg-white"><div className="flex items-center justify-between border-b border-slate-200 px-4 py-3"><div className="flex items-center gap-2"><Megaphone size={18} className="text-[#2563a8]"/><h2 className="font-bold">중요 공지</h2></div><Link href="/announcements" className="text-sm font-semibold text-[#2563a8] hover:underline">전체 보기</Link></div>{importantAnnouncement && <Link href="/announcements" className="block px-4 py-4 hover:bg-[#f8fbfe]"><p className="text-sm font-semibold leading-6 text-slate-800">{importantAnnouncement.title}</p><p className="mt-1.5 text-xs text-slate-500">{importantAnnouncement.author} · {importantAnnouncement.createdAt}</p></Link>}</section></div>{currentRole !== "MEMBER" && <ReviewQueue/>}<AppModal open={Boolean(selectedDate)} title={selectedDate ? `${selectedDate.slice(5, 7)}월 ${Number(selectedDate.slice(8))}일 일정` : "일정"} description={selectedItems.length ? `행사·업무·공지 ${selectedItems.length}개가 있어요.` : "이 날짜에는 등록된 일정이 없어요."} onClose={() => setSelectedDate(null)} footer={<Button onClick={() => setSelectedDate(null)}>닫기</Button>}>{selectedItems.length ? <div className="divide-y divide-slate-100">{selectedItems.map((item) => <Link key={item.id} href={item.href} onClick={() => setSelectedDate(null)} className="block py-3 first:pt-0 last:pb-0 hover:bg-[#f8fbfe]"><StatusBadge label={typeStatus[item.type]}/><h3 className="mt-2 text-sm font-bold leading-5 text-slate-800">{item.title}</h3><p className="mt-1.5 text-sm text-slate-500">{item.meta}</p><span className="mt-2 inline-block text-xs font-semibold text-[#2563a8]">상세 보기</span></Link>)}</div> : <p className="py-4 text-center text-sm text-slate-500">다른 날짜를 선택해 보세요.</p>}</AppModal></div>;
}
