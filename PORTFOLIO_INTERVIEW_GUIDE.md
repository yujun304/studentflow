# StudentFlow 포트폴리오·면접 준비 가이드

이 문서는 StudentFlow를 포트폴리오에서 설명하거나 기술 면접에서 발표할 때 사용할 수 있도록, 프로젝트의 실제 구현을 기준으로 정리한 안내서다. 구현되지 않은 기능을 완성된 것처럼 설명하지 않고 현재 동작 범위와 확장 방향을 구분한다.

## 1. 프로젝트를 한 문장으로 설명하기

> StudentFlow는 중학생 학생회의 공지, 업무 제출, 조 편성, 일정, 선착순 신청과 출석 기록을 하나의 권한 기반 흐름으로 연결한 폐쇄형 운영 플랫폼입니다.

### 30초 소개

학생회 활동은 메신저, 문서와 종이 명단에 정보가 흩어져 공지 누락, 마감 지연, 출석 기록 분실이 발생하기 쉽습니다. StudentFlow는 React와 FastAPI로 이 정보를 통합하고, 교사·회장단·부장단·일반 임원 역할에 따라 서버에서 접근 범위를 검사합니다. 특히 업무 제출과 검토, 조 편성 승인 후 개인 캘린더 자동 반영, 선착순 동시성 제어, 행사 출석 일괄 기록을 하나의 데이터 흐름으로 구현했습니다.

### 2분 소개

StudentFlow의 핵심은 기능의 개수보다 서로 다른 기능이 연결되는 방식이다. 관리자가 업무와 마감일을 등록하면 담당자에게만 업무가 노출되고, 제출 업무는 버전 단위로 보존된 뒤 관리자 검토를 거친다. 구조화된 조 편성 업무는 조장, 조원, 역할과 일시를 입력해 제출하며, 승인되면 조원별 리마인더와 행사 참여 명단이 자동 생성된다. 행사 당일에는 해당 참여자를 출석 체크리스트로 불러와 출석·결석·지각·조퇴를 일괄 저장하고, 변경자와 변경 내용을 감사 기록으로 남긴다.

인증은 짧은 수명의 Access JWT와 회전하는 Refresh JWT를 HttpOnly 쿠키로 전달한다. 쿠키 인증에서 발생할 수 있는 CSRF는 별도 토큰과 Origin 검사로 방어한다. 역할 정보만 프론트엔드 메뉴에 사용하는 것이 아니라, 모든 보호 API에서 현재 사용자, 활성 상태, 기수, 역할과 부서 범위를 다시 확인한다.

프로젝트의 기술 범위를 중학생 포트폴리오에 맞추기 위해 채팅과 실시간 메시지 기능은 제거했다. 개발과 배포는 Docker Compose로 PostgreSQL, FastAPI, Nginx 기반 React 정적 서버를 함께 실행한다.

## 2. 해결하려는 문제와 사용자

### 문제

- 공지와 마감일이 채팅에 묻힌다.
- 업무 담당자, 제출 여부와 검토 결과를 반복해서 확인해야 한다.
- 조 편성표와 실제 개인 일정이 분리되어 있다.
- 행사 참여 명단과 출석 기록이 종이나 별도 파일에 남는다.
- 역할과 부서에 따라 보여야 할 정보가 다르지만 메신저에서는 통제하기 어렵다.

### 사용자 역할

| 역할 | 주요 권한 |
|---|---|
| `MEMBER` | 자신에게 배정된 업무, 공지, 일정, 조 편성과 출석 기록 확인 |
| `DEPARTMENT_HEAD` | 담당 부서 범위의 업무·공지·행사·출석 관리와 제출 검토 |
| `EXECUTIVE_BOARD` | 기수 전체 범위의 운영 데이터 관리 |
| `TEACHER` | 전체 운영 권한, 사용자 계정과 역할·학년·부서 관리 |

프론트엔드의 메뉴 숨김은 편의를 위한 표현일 뿐 보안 경계가 아니다. 실제 권한 경계는 FastAPI 의존성과 각 도메인 쿼리에 있다.

## 3. 기술 스택과 선택 이유

### Frontend

- React 19, TypeScript, Vite
- Vite MPA: 화면별 HTML 엔트리와 전체 문서 이동
- `useApiData`: `useEffect`와 `useState` 기반의 단순 서버 데이터 로딩
- 반응형 CSS 및 PWA 기본 구성

React 컴포넌트는 서버 데이터를 표시하고 사용자 입력을 API에 전달하는 비교적 얇은 계층으로 유지했다. 업무·공지·사용자·달력 데이터는 공통 `useApiData` 훅으로 불러오며, 변경 성공 후 현재 HTML 문서를 다시 불러온다. MPA에서는 페이지별 클라이언트 캐시를 유지할 실익이 작아 별도의 서버 상태 라이브러리를 사용하지 않는다.

### Backend

- Python 3.12
- FastAPI, Pydantic
- SQLAlchemy 2 비동기 세션
- Alembic 마이그레이션
- PyJWT, Argon2 기반 비밀번호 해시

FastAPI는 타입 힌트와 Pydantic 모델을 OpenAPI 계약으로 연결하기 쉽고, `async/await` 기반 DB 처리를 일관된 실행 모델로 구성할 수 있어 선택했다.

### Database and deployment

- PostgreSQL 16
- 개발 단위 테스트 일부는 인메모리 SQLite 사용
- Docker Compose: PostgreSQL, Backend, Frontend
- Nginx: React 정적 파일 제공과 `/api` 리버스 프록시
- 로컬 비공개 업로드 볼륨, 추후 S3 교체를 고려한 저장소 설정 계약

## 4. 전체 아키텍처

```text
Browser / PWA
    │
    │ HTTPS, JSON REST
    ▼
Nginx :8081
    ├── /           → 화면별 React 정적 HTML 문서
    └── /api/v1/*   → FastAPI :8000
                         │
                         ├── Pydantic 입력 검증
                         ├── JWT·CSRF·역할/범위 검사
                         ├── 도메인 라우터
                         ├── SQLAlchemy async
                         └── 비공개 파일 접근 검사
                                   │
                                   ▼
                              PostgreSQL 16
```

백엔드는 다음처럼 역할을 나눈다.

- `backend/app/api`: HTTP 요청, 권한 검사, 도메인 흐름
- `backend/app/models/entities.py`: SQLAlchemy 엔티티와 제약 조건
- `backend/app/schemas.py`: 요청·응답 데이터 계약
- `backend/app/services`: 여러 API가 공유하는 조 편성·캘린더 규칙
- `backend/app/core`: 설정, DB 세션, 인증, 오류, 파일 저장소
- `backend/alembic`: 운영 DB 스키마 변경 이력
- `backend/tests`: 권한, 계약과 주요 도메인 흐름 테스트

## 5. 핵심 데이터 모델

### 조직과 사용자

- `Term`: 학생회 기수와 현재 기수 여부
- `Department`: 부서
- `User`: 이메일, 이름, 역할, 부서, 기수, 1~3학년, 활성 상태
- `RefreshSession`: Refresh Token 해시, 만료와 폐기 상태

사용자를 삭제하는 대신 비활성화할 수 있도록 설계해 과거 제출·출석 기록의 작성자 관계를 보존한다. 학년은 학생에게는 1~3, 교사에게는 `NULL`을 허용하며 DB 체크 제약과 Pydantic 범위 검사를 함께 사용한다.

### 업무와 제출

- `Task`, `TaskAssignee`
- `Submission`, `SubmissionVersion`, `StoredFile`
- 업무 유형: 간단 업무, 제출 업무, 조 편성
- 상태: 해야 할 일, 진행 중, 검토 중, 완료, 반려

제출물을 덮어쓰지 않고 `SubmissionVersion`에 차수별로 저장한다. 검토 화면은 각 제출의 최신 버전을 가져오며, 승인·반려 결과가 업무 상태에 연결된다.

### 조 편성과 일정

- `Team`, `TeamMember`
- `Reminder`
- `Event`, `EventParticipant`

조 편성에는 조 이름, 조장, 조원, 역할, 설명, 활동 일시, 관련 행사와 업무가 들어간다. 조 편성 제출이 승인되면 다음 작업을 하나의 트랜잭션에서 처리한다.

1. 조 편성의 승인 시각 기록
2. 조원별 `TEAM` 리마인더 생성
3. 기존 조 리마인더와 중복되지 않도록 사용자·원본 조 복합 유일성 유지
4. 관련 행사가 있다면 조원을 `EventParticipant`에 중복 없이 추가
5. 사용자의 2개월 달력에 조 일정과 행사가 함께 노출

### 공지와 선착순

- `Notice`, `NoticeRecipient`, `NoticeApplication`
- 공지 유형: 일반, 행사, 설문, 선착순

선착순 신청은 공지 행을 `SELECT ... FOR UPDATE`로 잠근 상태에서 승인 인원을 계산한다. 정원이 남으면 승인, 정원이 찼고 대기가 허용되면 대기 상태를 부여한다. 취소된 자리가 생기면 가장 앞선 대기자를 승격한다. 이 방식은 애플리케이션 메모리의 카운터보다 다중 요청에서 안전하다.

### 출석과 감사 기록

- `Attendance`: 행사·사용자 복합 유일성, 단일 출석 상태, 메모, 기록자, 수정 시각
- `AuditLog`: 변경자, 동작, 대상, 이전/새 상태 상세

행사 참여자는 한 행사에서 하나의 상태만 가질 수 있다. 저장 API는 일부 또는 여러 참여자의 상태를 한 번에 upsert하고, 실제 값이 변경됐을 때만 감사 기록을 추가한다.

## 6. 주요 기능의 요청 흐름

### 로그인과 토큰 갱신

```text
GET /auth/csrf
  → CSRF 쿠키와 응답 토큰 수신
POST /auth/login + X-CSRF-Token
  → Access JWT(HttpOnly) + Refresh JWT(HttpOnly) + 새 CSRF 쿠키
GET /auth/me
  → Access JWT 검증 후 DB에서 활성 사용자·session_version 재확인
Access 만료
  → POST /auth/refresh
  → 기존 Refresh Session 폐기 + 새 토큰 쌍 발급
```

JWT에 역할을 넣고 그대로 신뢰하는 대신 `sub`와 세션 버전을 사용해 DB 사용자를 다시 조회한다. 사용자의 역할·활성 상태·비밀번호 같은 보안 정보가 바뀌면 `session_version`을 증가시키고 기존 Refresh Session을 폐기해 이미 발급된 토큰을 무효화한다.

### 업무 제출과 검토

```text
관리자 업무 등록
  → 담당자 범위 검증
담당자 제출
  → Submission + SubmissionVersion 생성
  → Task 상태 REVIEW
관리자 승인/반려
  → Submission 결과와 Task 상태 동시 변경
```

부장단은 자신이 만든 업무만 검토할 수 있고, 회장단과 교사는 전체 기수 범위를 관리한다. 담당자가 아닌 사용자는 제출 엔드포인트를 호출할 수 없다.

### 자동 조 편성 반영

```text
TEAM_FORMATION 업무 생성
  → 담당자 지정
담당자가 구조화된 조 편성 제출
  → 조장·조원·일정·역할 저장
관리자가 승인
  ├── Team.approved_at 기록
  ├── 조원별 Reminder 생성
  └── 관련 EventParticipant 생성
사용자 대시보드
  → 조 일정 + 행사 일정 확인
```

검토 전 편성안이 바로 확정 일정으로 보이지 않도록 승인 시점을 자동 반영의 경계로 정했다.

### 출석 체크리스트

- `GET /api/v1/events/{event_id}/attendance`
  - 행사 정보, 행사 당일/시간 여부, 상태별 요약, 참여자별 상태 반환
- `PUT /api/v1/events/{event_id}/attendance`
  - 여러 참여자의 상태와 메모 일괄 저장
- `POST /api/v1/events/{event_id}/attendance/mark-all-present`
  - 전체 참여자를 출석으로 처리

관리자는 전체 명단을 보고 수정할 수 있다. 일반 사용자는 같은 조회 경로에서 자신의 기록만 받는다. 요청에 행사 참여자가 아닌 사용자가 포함되거나 동일 사용자가 중복되면 전체 요청을 거절한다.

### 통합 달력

`GET /api/v1/calendar?start=YYYY-MM-DD&end=YYYY-MM-DD`는 최대 93일 범위에서 다음 데이터를 합친다.

- 행사와 캠페인
- 업무·제출·조 편성 마감
- 승인된 조 일정
- 개인 일정

원본 엔티티를 하나의 캘린더 테이블에 복제하지 않고 조회 시 통합 응답 모델로 변환한다. 개인 일정만 수정·삭제할 수 있으며 행사나 업무 일정은 각 원본 기능에서 관리한다. 모든 시간 계산은 설정된 `Asia/Seoul` 기준과 UTC 저장을 구분한다.

## 7. 보안 설계

### 쿠키 기반 JWT와 CSRF

- Access/Refresh Token은 `HttpOnly`로 JavaScript 접근 차단
- `SameSite=Lax`, 운영 HTTPS에서는 `Secure=true`
- 상태 변경 요청은 CSRF 쿠키와 `X-CSRF-Token` 헤더를 상수 시간 비교
- Origin이 설정된 프론트엔드 주소와 다르면 거절
- Refresh Token 원문은 DB에 저장하지 않고 SHA-256 digest만 저장
- 비밀번호는 Argon2 권장 설정으로 해시

HttpOnly 쿠키는 토큰 탈취 위험을 줄이지만 브라우저가 자동 전송하므로 CSRF 방어가 별도로 필요하다. 이 두 특성을 함께 설명하는 것이 중요하다.

### 권한과 데이터 범위

- 공통 역할 검사는 FastAPI dependency로 수행
- 도메인별로 기수, 부서, 작성자, 담당자, 행사 참여 여부를 추가 검사
- 사용자 ID 목록을 받는 API는 현재 기수·활성 사용자 여부를 서버에서 재검증
- 파일 다운로드는 DB 관계와 사용자 권한을 확인한 뒤 스트리밍

“프론트에서 메뉴를 숨겼다”는 보안 설명이 아니다. 공격자는 API를 직접 호출할 수 있으므로 모든 보호 API가 독립적으로 권한을 확인해야 한다.

## 8. 데이터 무결성과 동시성

DB 유일성 제약을 주요 불변 조건의 최종 방어선으로 사용한다.

- 사용자 이메일 유일성
- 행사별 참여자 중복 금지
- 업무별 담당자 중복 금지
- 공지별 수신자·신청자 중복 금지
- 조별 조원 중복 금지
- 행사·사용자별 출석 기록 하나
- 사용자·원본 조별 리마인더 하나

API 검증은 친절한 오류 메시지를 제공하고, DB 제약은 경쟁 요청이나 구현 실수에도 데이터가 깨지지 않도록 한다. 선착순 신청처럼 읽기 후 쓰기가 연결되는 기능은 행 잠금과 트랜잭션을 사용한다.

## 9. 프론트엔드 설계 포인트

- 인증 사용자 쿼리를 애플리케이션 진입점에서 관리
- 로그인 성공 시 `me` 캐시를 직접 설정하고 `/`로 replace 이동
- 로그아웃 시 인증 외 캐시를 제거하고 `/login`으로 이동
- API 모듈에서 CSRF 토큰 요청을 하나의 Promise로 공유해 동시 변경 요청의 중복 발급 방지
- 401 응답 시 Refresh 요청 후 원래 요청 한 번 재시도
- 공지 수신자·업무 담당자는 전체 또는 1·2·3학년 빠른 선택 후 개별 조정
- 이번 달·다음 달 달력을 반응형으로 표시하고 일정 종류별 색상 구분

## 10. 테스트 전략

### Backend

- pytest + pytest-asyncio
- SQLAlchemy 모델 기반 인메모리 SQLite 단위·통합 테스트
- 인증, CSRF, 저장소 경로, 공지 수정, 업무 제출, 조 편성·달력, 학년 필터, 출석 일괄 처리 검증
- PostgreSQL Docker 환경에서 Alembic 적용 및 실제 API E2E 별도 검증

SQLite 테스트만으로 PostgreSQL 고유 동작을 모두 보장할 수 없으므로 enum 마이그레이션, 행 잠금과 실제 timezone 반환은 PostgreSQL 환경에서도 확인한다.

### Frontend

- Vitest
- CSRF 요청 공유와 로그인 후 토큰 재동기화 테스트
- TypeScript 프로젝트 빌드와 Vite production build를 배포 전 수행

### 현재 확인 명령

```powershell
Set-Location backend
uv run ruff check app tests alembic
uv run pytest tests -q

Set-Location ..\frontend
npm test
npm run build

Set-Location ..
docker compose up -d --build
```

현재 기준 백엔드 테스트 18개와 프론트엔드 테스트 3개가 통과하며, Docker 환경의 `/api/v1/health`와 실제 주요 흐름을 함께 확인했다.

## 11. 실제 트러블슈팅 사례

### 사례 1: 로그인 직후 첫 변경 요청에서 CSRF 실패

증상은 로그인 성공 후 화면 이동이 즉시 되지 않고 “요청 검증이 실패했습니다”가 나온 뒤 새로고침하면 정상 동작하는 것이었다. 로그인 과정에서 CSRF 쿠키가 회전했지만 프론트 메모리에는 이전 토큰이 남아 있었다. 로그인·갱신·로그아웃 후 메모리 토큰을 초기화하고 새 토큰을 다시 가져오도록 수정했다. 동시에 여러 변경 요청이 발생할 때 CSRF 요청을 공유 Promise로 합쳐 경쟁 상태를 줄였다.

면접에서는 “쿠키가 바뀌었는데 프론트 상태가 이전 값을 들고 있던 동기화 문제”로 설명하면 된다.

### 사례 2: MPA 직접 접근 경로가 루트 문서로 처리됨

`/login/`, `/tasks/` 같은 경로가 각각의 HTML 문서를 제공해야 하므로 Vite Multi-Page Build 입력에 모든 문서를 등록했다. Nginx도 루트 `index.html`로 되돌리는 SPA fallback을 제거하고 `$uri/index.html`을 찾도록 구성해, 새로고침과 직접 접근이 해당 화면 문서를 그대로 반환하게 했다.

### 사례 3: 시간대 데이터와 날짜 정렬

Windows 환경에는 IANA timezone 데이터가 없을 수 있어 `Asia/Seoul` 로딩이 실패했다. `tzdata`를 런타임 의존성으로 추가했다. 또한 SQLite가 timezone-aware 컬럼을 naive datetime으로 반환하는 경우 행사 일정과 리마인더 정렬이 실패해, 정렬 키를 UTC aware datetime으로 정규화했다.

### 사례 4: 실제 E2E에서만 발견된 생성 API 오류

사용자와 행사 생성 API가 `model_dump()` 결과와 명시적 키워드로 같은 필드를 두 번 전달해 `multiple values for keyword argument` 오류를 냈다. 해당 화면이 처음에는 없어 단위 흐름에서 드러나지 않았다. 실제 관리자 계정으로 API 전체 흐름을 검증하며 발견했고, 명시적으로 처리할 필드를 dump 제외 목록에 넣고 회귀 테스트를 추가했다.

### 사례 5: 조 편성과 출석의 도메인 연결

조 일정 리마인더만 만들면 캘린더에는 보이지만 행사 출석 명단에는 나타나지 않았다. 승인된 조가 행사와 연결된 경우 조원을 `EventParticipant`에 추가하도록 서비스 계층을 확장했다. 기존 참여자는 유지하고 누락된 사용자만 추가해 복합 유일성 충돌을 피했다.

## 12. 면접 예상 질문과 답변 핵심

### 왜 FastAPI를 선택했나요?

Pydantic 입력 검증과 OpenAPI 생성이 자연스럽고 async SQLAlchemy를 사용하기 적합했다. 다만 CPU 집약 작업에 async가 성능을 자동으로 높여주는 것은 아니며, 이 프로젝트에서는 DB·네트워크 대기 중심 작업에 이점이 있다.

### JWT인데 왜 DB를 매번 조회하나요?

JWT만 신뢰하면 계정 비활성화나 역할 변경이 Access Token 만료 전까지 반영되지 않는다. 이 프로젝트는 토큰의 사용자 ID와 세션 버전을 검증한 뒤 DB에서 현재 활성 상태와 권한 범위를 확인해 즉시 정책 변경을 반영한다. 완전한 stateless 인증보다 폐쇄형 운영 도구의 통제 가능성을 우선했다.

### Refresh Token은 어떻게 보호하나요?

HttpOnly 쿠키로 전달하고 DB에는 원문 대신 digest만 저장한다. 갱신 때 기존 세션을 폐기하고 새 토큰을 발급한다. 사용자 보안 정보가 변경되면 세션 버전을 증가시켜 기존 Access Token도 무효화한다.

### HttpOnly 쿠키를 쓰는데 왜 CSRF 토큰이 필요한가요?

HttpOnly는 JavaScript 탈취를 막지만 쿠키는 브라우저가 요청에 자동 첨부한다. 따라서 공격 사이트가 사용자의 브라우저를 이용해 상태 변경 요청을 보내는 CSRF 가능성이 남는다. 별도 토큰과 Origin 검사를 함께 사용했다.

### 역할 기반 권한을 어떻게 구현했나요?

공통 역할은 `require_roles` dependency로 검사하고, 부서·기수·작성자·담당자처럼 리소스별 범위는 실제 엔티티를 조회한 뒤 각 API에서 검사한다. 응답에서 메뉴를 숨기는 것만으로 권한을 구현하지 않았다.

### 선착순 정원 초과는 어떻게 막나요?

공지 행을 트랜잭션 안에서 `FOR UPDATE`로 잠그고 현재 승인 인원을 센 뒤 상태를 결정한다. 사용자·공지 복합 유일성으로 중복 신청도 막는다. 단순히 API 서버 메모리에서 숫자를 증가시키면 여러 프로세스나 동시 요청에서 정원이 초과될 수 있다.

### 조 편성 자동 반영이 중복 리마인더를 만들지 않나요?

리마인더에 원본 유형과 원본 조 ID를 저장하고 사용자·원본 복합 유일성을 둔다. 조 편성이 갱신될 때 기존 원본 리마인더를 지우고 현재 조원 기준으로 다시 동기화한다.

### 출석 API에서 왜 전체 명단을 매번 생성하나요?

행사 참여 명단이 기준 데이터이고, 아직 저장되지 않은 사용자는 `PENDING`으로 계산한다. 따라서 행사 생성 시 모든 사용자에 대한 출석 행을 미리 만들 필요가 없다. 실제 상태가 기록될 때만 Attendance 행을 만들며, 체크리스트 응답에서 참여자와 기록을 합친다.

### 감사 기록에는 무엇을 남기나요?

출석 상태나 메모가 실제로 달라진 경우 변경자, 행사, 대상 사용자, 이전 상태·메모와 새 상태·메모를 남긴다. 현재 감사 로그 조회 UI와 일반화된 감사 API는 확장 범위다.

### 비동기 DB를 쓰면서 주의한 점은 무엇인가요?

요청마다 AsyncSession을 만들고 트랜잭션 경계를 명시한다. 관련 데이터 변경은 가능한 한 한 번의 commit으로 묶는다. 반복문 안의 불필요한 쿼리를 줄이기 위해 사용자·담당자·조원 ID를 집합으로 일괄 조회하고 매핑한다.

### SQLite 테스트와 PostgreSQL 운영의 차이는 어떻게 다루나요?

빠른 도메인 테스트에는 SQLite를 사용하지만 PostgreSQL enum, `FOR UPDATE`, timezone과 Alembic은 Docker PostgreSQL에서도 검증한다. SQLite 테스트 통과만으로 운영 DB 호환성을 주장하지 않는다.

## 13. 포트폴리오 시연 순서

면접 시 모든 메뉴를 돌아다니기보다 하나의 연결된 업무 흐름을 보여주는 것이 좋다.

1. 교사 계정으로 로그인한다.
2. 관리 화면에서 학년과 역할이 지정된 학생 계정을 보여준다.
3. 행사와 조 편성 업무를 만들고 담당자를 지정한다.
4. 담당자 계정으로 조 이름, 조원, 역할, 일시를 입력해 제출한다.
5. 교사 계정에서 제출 버전을 검토하고 승인한다.
6. 조원 계정의 달력에 조 일정과 행사가 자동 표시되는 것을 보여준다.
7. 행사 출석 체크리스트에서 학년·부서와 참여자 명단을 확인한다.
8. 일부 사용자를 지각/결석으로 저장하고 상태 요약이 바뀌는 것을 보여준다.
9. 선착순 공지의 정원과 대기 처리를 설명한다.
10. 마지막에 네트워크 구조, 보안 경계와 테스트 결과를 짧게 보여준다.

## 14. 코드 리뷰 동선

면접관이 코드를 보고 싶다고 할 때 다음 순서가 이해하기 쉽다.

1. `backend/app/main.py`: 라우터와 API 마운트
2. `backend/app/core/security.py`: 인증, CSRF, 역할 dependency
3. `backend/app/models/entities.py`: 주요 데이터 관계와 제약
4. `backend/app/api/tasks.py`: 업무 제출·검토 흐름
5. `backend/app/services/team_calendar.py`: 조 승인 후 일정·행사 참여자 동기화
6. `backend/app/api/attendance.py`: 체크리스트, 일괄 upsert와 감사 기록
7. `backend/app/api/notices.py`: 수신 범위와 선착순 트랜잭션
8. `frontend/src/api.ts`: CSRF, Refresh, 공통 오류 처리
9. `frontend/src/App.tsx`, `navigation.ts`, `Calendar.tsx`: MPA 문서 선택과 통합 달력
10. `backend/tests`: 주요 도메인 회귀 테스트

## 15. 현재 구현 상태와 한계

### 동작하는 핵심 범위

- 관리자 생성형 폐쇄 계정, 역할·부서·학년
- Access/Refresh JWT 쿠키, Refresh Session, CSRF
- 대시보드와 2개월 통합 달력, 개인 일정 CRUD
- 행사·캠페인 기본 API
- 업무 배정, 상태 변경, 제출 버전, 파일 업로드, 승인·반려
- 구조화된 조 편성, 승인 후 조원 캘린더·행사 참여자 자동 반영
- 공지 수신자, 선착순 승인·대기·취소
- 행사 출석 체크리스트, 일괄 저장, 전체 출석, 감사 기록 생성
- Docker Compose 실행과 Alembic 마이그레이션

### 확장 예정 또는 제한된 범위

- 실제 S3 오브젝트 스토리지 어댑터는 아직 없고 비공개 로컬 볼륨을 사용한다.
- PWA 설치 뼈대는 있으나 실제 Push 발송은 구현 범위 밖이다.
- 출석 체크리스트 API는 동작하지만 전용 모바일 출석 화면은 아직 없다.
- 감사 기록은 출석 변경 시 생성되지만 범용 조회 화면/API는 준비 단계다.
- 아카이브 등 일부 후속 API는 명시적으로 `501 feature_not_ready`를 반환한다.
- 자동화 테스트는 핵심 도메인을 중심으로 하며 전체 브라우저 E2E 스위트는 아직 없다.

한계를 숨기기보다 “왜 지금 범위를 이렇게 정했고, 다음 단계에서 어떤 기술로 해결할지”를 설명하는 편이 신뢰를 준다.

## 16. 다음 단계의 기술 계획

1. 출석 체크리스트용 모바일 UI와 이름 검색·부서·미처리 필터 구현
2. S3 호환 저장소 어댑터와 presigned URL이 아닌 권한 확인형 다운로드 정책 검증
3. Push 구독 관리와 알림 발송 워커 설계
4. 감사 로그 조회 및 교사 전용 필터 API 구현
5. Playwright 기반 로그인·업무 제출·조 승인·출석 브라우저 E2E 추가
6. 관측성을 위한 구조화 로그, 요청 ID, 오류 지표와 백업·복구 절차 추가

외부 브로커나 작업 큐는 필요성이 확인된 뒤 도입한다. 현재 규모에서 Redis, Celery 같은 구성요소를 먼저 추가하면 운영 복잡성만 늘어날 수 있다.

## 17. 이력서·포트폴리오용 문장 예시

- React·TypeScript와 FastAPI·PostgreSQL로 역할·부서·기수 범위 권한을 적용한 폐쇄형 학생회 운영 플랫폼 개발
- HttpOnly Access/Refresh JWT, Refresh Token rotation, 세션 버전 및 CSRF·Origin 검증을 결합한 인증 흐름 구현
- 업무 제출 버전 관리와 승인 기반 조 편성 자동화를 설계해 조원별 캘린더 및 행사 참여 명단을 트랜잭션으로 동기화
- PostgreSQL 행 잠금과 복합 유일성 제약을 활용해 선착순 정원 초과와 중복 신청 방지
- 행사 참여자 기반 출석 체크리스트, 상태 일괄 upsert, 전체 출석 처리 및 변경 감사 로그 구현
- Alembic 마이그레이션, Docker Compose, pytest·Vitest 및 실제 PostgreSQL API E2E로 배포 전 검증

## 18. 면접 직전 체크리스트

- 프로젝트를 30초와 2분 버전으로 각각 말할 수 있는가?
- 가장 중요한 사용자 문제를 기능 목록 없이 설명할 수 있는가?
- 인증에서 HttpOnly와 CSRF가 각각 막는 공격을 구분할 수 있는가?
- 서버 권한 검사 예시를 역할·부서·기수 관점에서 설명할 수 있는가?
- 조 편성 승인부터 캘린더·행사 참여자 생성까지의 트랜잭션을 그릴 수 있는가?
- 선착순 신청에서 행 잠금이 필요한 이유를 설명할 수 있는가?
- SQLite 테스트와 PostgreSQL 운영 환경의 차이를 인정하고 검증 방법을 말할 수 있는가?
- 실제 해결한 버그 하나를 증상, 원인, 수정, 재발 방지 순서로 설명할 수 있는가?
- 아직 구현하지 않은 범위를 솔직하게 말하고 다음 설계를 제안할 수 있는가?

가장 좋은 답변 구조는 다음과 같다.

> 문제 상황 → 선택한 설계 → 다른 대안과 트레이드오프 → 실제 구현 → 검증 방법 → 현재 한계와 다음 단계

이 구조로 답하면 단순히 기술 이름을 나열하는 대신 문제를 정의하고 검증 가능한 방식으로 해결한 경험을 보여줄 수 있다.
