# StudentFlow 프로젝트 용량 분석 및 기술 보고서

- 분석 대상: `C:\cou`
- 분석 일자: 2026-07-25
- 분석 기준: 저장소의 실제 파일, FastAPI 라우터, SQLAlchemy 모델, React 화면, 테스트 및 빌드 결과

## 1. 요약

StudentFlow는 중학생 학생회의 행사, 업무, 제출물, 공지, 출석, 조 편성 및 개인 일정을 하나의 폐쇄형 웹 애플리케이션에서 관리하기 위한 플랫폼이다. 프론트엔드는 화면별 HTML 문서를 가진 React 기반 MPA(Multi-Page Application), 백엔드는 FastAPI 기반 비동기 REST API, 데이터베이스는 PostgreSQL을 사용한다.

정리 전 디렉터리의 실제 크기는 **276,708,388바이트(263.89MiB)**였다. 이미지 때문이 아니라 `node_modules`, Python 가상환경, Python 패키지 다운로드 캐시가 전체 용량의 대부분을 차지했다. 2026-07-25 캐시와 재생성 가능한 개발 의존성을 제거한 뒤에는 약 **0.61MiB**만 남았다.

현재 완성도가 높은 핵심 범위는 다음과 같다.

- 쿠키 기반 JWT 인증과 CSRF 방어
- 역할·부서·기수 기반 백엔드 권한 검사
- 개인 대시보드와 통합 달력
- 행사 조회 및 참여자 관리 API
- 업무 칸반, 담당자 배정, 제출 버전, 파일 제출, 승인·반려
- 조 편성 제출·승인 및 개인 달력 연동
- 공지 작성·수신자 지정·선착순 신청
- 출석 체크리스트 API와 변경 감사 로그
- 교사의 사용자 계정 생성

회의록, 댓글, 빠른 메모, 저장 항목, 알림, 푸시 구독, 기수 아카이브, 감사 로그 조회는 데이터 모델 또는 API 계약이 일부 존재하지만 현재 요청 시 `501 feature_not_ready`를 반환한다. 실제 PWA 푸시 발송과 S3 오브젝트 스토리지도 구현되지 않았다.

## 2. 200MB 이상인 이유

### 2.1 정리 전 실제 용량 구성

| 경로 | 크기 | 주요 내용 | 재생성 가능 여부 |
|---|---:|---|---|
| `.data` | 91.79MiB | `uv` 패키지 캐시 90.69MiB, 테스트용 SQLite DB 약 1.10MiB | 캐시는 가능, DB는 확인 필요 |
| `frontend` | 91.49MiB | `node_modules` 91.04MiB, 빌드 결과 0.30MiB, 소스 약 0.05MiB | 의존성과 빌드 결과는 가능 |
| `.venv` | 79.83MiB | Python 실행 환경과 설치 패키지 | 가능 |
| `backend` | 0.67MiB | 백엔드 소스, 테스트, 마이그레이션 및 `__pycache__` | 소스 제외 일부 가능 |
| 루트 문서·설정 | 약 0.12MiB | README, 설계 문서, Compose 설정 등 | 원본 |

세 개의 대형 영역만 합쳐도 약 263MiB다.

1. `frontend/node_modules`: React, TypeScript, Vite, Vitest, ESLint와 Windows용 네이티브 빌드 모듈을 포함한다.
2. `.venv`: FastAPI, SQLAlchemy, Pydantic, asyncpg, pytest, Ruff 등의 실제 설치본이다.
3. `.data/uv-cache`: 같은 Python 패키지를 다시 설치할 때 다운로드를 생략하기 위한 캐시다. `.venv`와 일부 내용이 중복된다.

### 2.2 정리 결과

2026-07-25 다음 재생성 가능 항목을 삭제했다.

- `.venv`
- `frontend/node_modules`
- `frontend/dist`
- `.data`의 `uv` 캐시, 마이그레이션 테스트 DB 및 Docker 보조 설정
- 루트와 백엔드의 Ruff·Pytest 캐시
- 모든 `__pycache__`, `.pyc`, `.pyo`, `.log`, `*.tsbuildinfo`

정리 후 전체 파일은 88개, 전체 크기는 약 0.61MiB다. 소스, 테스트 원본, `package-lock.json`, `uv.lock`, Docker 설정, Alembic 마이그레이션, 프로젝트 문서는 보존했다.

### 2.3 정리 전 큰 단일 파일

| 파일 | 크기 | 설명 |
|---|---:|---|
| `.venv/Scripts/ruff.exe` | 30.82MiB | Python 정적 검사 도구 |
| `.data/uv-cache/.../ruff.exe` | 30.82MiB | 위 Ruff의 설치 캐시 복사본 |
| `frontend/node_modules/@rolldown/...node` | 19.56MiB | Vite 빌드 체인의 Windows 네이티브 모듈 |
| `frontend/node_modules/lightningcss-...node` | 9.06MiB | CSS 처리용 Windows 네이티브 모듈 |
| `frontend/node_modules/typescript/lib/typescript.js` | 8.69MiB | TypeScript 컴파일러 |
| `.data/uv-cache/.../pydantic-core...` | 8.16MiB | Pydantic 코어 패키지 캐시 |

`frontend/public`에는 1KB 미만의 `icon.svg`, PWA manifest, 서비스 워커만 있다. 따라서 이미지 자산은 전체 용량에 사실상 영향을 주지 않는다.

### 2.4 왜 `.gitignore`가 있는데도 큰가

`.gitignore`에는 `.venv/`, `node_modules/`, `.data/`, `frontend/dist/`가 이미 제외되어 있다. 이는 Git 추적만 막는다. Windows 탐색기나 일반 압축 프로그램으로 `C:\cou` 폴더 전체를 압축하면 무시된 디렉터리도 그대로 포함된다.

현재 `C:\cou\.git`은 유효한 Git 저장소로 인식되지 않으므로 `git archive` 방식도 바로 사용할 수 없다. 제출·이동용 압축 파일을 만들 때는 다음 경로를 제외해야 한다.

- `.venv`
- `.data/uv-cache`
- `frontend/node_modules`
- `frontend/dist`
- `__pycache__`
- `.ruff_cache`

단, `.data` 전체를 일괄 삭제하면 테스트 DB나 향후 업로드 파일까지 없어질 수 있다. 캐시만 지우려면 `.data/uv-cache`만 대상으로 해야 한다. `node_modules`, `.venv`, `frontend/dist`는 각각 `npm install` 또는 `npm ci`, `uv sync`, `npm run build`로 재생성할 수 있다.

## 3. 전체 시스템 윤곽

```text
브라우저 / 설치형 PWA
        │
        │ HTTPS, JSON REST, HttpOnly 쿠키, CSRF 헤더
        ▼
Nginx 정적 파일 서버 및 /api 리버스 프록시
        │
        ▼
FastAPI /api/v1
        ├─ 인증·권한 의존성
        ├─ 도메인 라우터
        ├─ Pydantic 요청·응답 검증
        ├─ SQLAlchemy AsyncSession
        └─ 권한 검사 후 파일 스트리밍
              │
              ├─ PostgreSQL 16
              └─ 비공개 로컬 파일 볼륨
```

프론트엔드는 작은 `useApiData` 훅으로 서버 데이터를 가져오고, 변경 요청이 성공하면 현재 HTML 문서를 다시 불러온다. 별도의 클라이언트 캐시나 전역 상태 관리 계층은 사용하지 않는다. 백엔드는 기능별 FastAPI `APIRouter`와 공통 보안 의존성으로 구성되어 있으며, SQLAlchemy 2.x의 비동기 세션으로 PostgreSQL에 접근한다. Alembic이 스키마 버전을 관리한다.

## 4. 기술 스택과 계층

### 4.1 프론트엔드

- React 19
- TypeScript 5
- Vite 8
- Vite Multi-Page Build
- React 기본 훅(`useState`, `useEffect`)
- Vitest 및 Testing Library
- 반응형 CSS와 모바일 하단 내비게이션
- PWA manifest 및 서비스 워커 등록

프론트엔드는 React Router를 사용하지 않는다. 대시보드, 로그인, 행사, 업무, 업무 편집, 공지, 공지 편집, 검토, 더보기, 관리 화면에 각각 별도의 HTML 엔트리가 있으며 일반 링크 이동 시 새 문서를 요청한다. `frontend/src/App.tsx`는 각 문서의 `data-page` 값에 맞는 화면을 렌더링하고, `navigation.ts`는 문서 경로를 관리한다. API 호출 공통 로직은 `frontend/src/api.ts`, 달력은 `Calendar.tsx`, 조 편성 폼은 `TeamFormationForm.tsx`, 사용자 다중 선택은 `UserAudienceSelect.tsx`에 분리되어 있다.

### 4.2 백엔드

- Python 3.12
- FastAPI
- Pydantic / pydantic-settings
- SQLAlchemy 2.x Async ORM
- asyncpg
- Alembic
- PyJWT
- Argon2 기반 `pwdlib`
- Uvicorn

`backend/app/api`가 HTTP 엔드포인트, `backend/app/models/entities.py`가 데이터 모델, `backend/app/core`가 인증·DB·오류·파일 저장소, `backend/app/services`가 조 편성과 달력 사이의 도메인 로직을 담당한다.

### 4.3 데이터베이스와 마이그레이션

운영 DB는 PostgreSQL 16이다. 테스트에서는 SQLite 비동기 드라이버도 사용한다. Alembic 마이그레이션은 다음 순서로 구성된다.

1. `0001_initial`: 초기 도메인 테이블
2. `0002_team_calendar`: 조 편성·달력 연계
3. `0003_user_grade`: 중학교 학년 필드
4. `0004_remove_chat`: 채팅 관련 테이블과 파일 연결 제거

프로젝트 정책에 맞게 채팅과 WebSocket은 제거되어 있다.

## 5. 사용자 역할과 권한 모델

| 역할 | 의미 | 주요 범위 |
|---|---|---|
| `MEMBER` | 일반 임원 | 본인에게 배정되거나 수신된 행사·업무·공지 중심 |
| `DEPARTMENT_HEAD` | 부장단 | 자기 부서 또는 본인이 생성한 데이터 관리 |
| `EXECUTIVE_BOARD` | 회장단 | 현재 기수의 전역 관리 기능 |
| `TEACHER` | 담당 선생님 | 전역 관리 및 사용자·부서·기수 관리 |

권한은 프론트엔드 메뉴 숨김에만 의존하지 않는다. 보호 API에서 다음 조건을 다시 검사한다.

- 로그인 사용자와 활성 상태
- JWT의 세션 버전과 DB의 현재 세션 버전
- 역할
- 현재 기수(`term_id`)
- 부서(`department_id`)
- 행사 참여자 여부
- 업무 담당자 여부
- 데이터 생성자 또는 관리자 여부

교사가 사용자의 역할, 활성 상태, 부서 또는 비밀번호를 바꾸면 해당 사용자의 모든 Refresh Session을 폐기하고 `session_version`을 증가시켜 기존 Access Token도 무효화한다.

## 6. 기능별 구현 현황

상태 표기의 의미는 다음과 같다.

- **사용 가능**: 백엔드와 현재 프론트 화면에서 주요 흐름을 사용할 수 있음
- **API 사용 가능**: 백엔드는 구현되었지만 현재 프론트 화면이 없거나 일부만 연결됨
- **부분 구현**: 일부 하위 흐름만 동작
- **계약만 존재**: 모델·스키마·라우트가 있으나 API가 501을 반환

### 6.1 인증과 세션 — 사용 가능

- 공개 회원가입 없이 관리자가 계정을 생성한다.
- 이메일과 비밀번호로 로그인한다.
- Access Token과 Refresh Token을 JavaScript에서 읽을 수 없는 HttpOnly 쿠키에 저장한다.
- Access Token 기본 수명은 15분, Refresh Token은 14일이다.
- Refresh Token Rotation을 적용하여 갱신할 때 기존 Refresh Session을 폐기하고 새 토큰을 발급한다.
- 로그아웃 시 서버의 Refresh Session을 폐기하고 인증 쿠키를 제거한다.
- 프론트 API 클라이언트는 401 응답 시 한 번 토큰 갱신을 시도한 뒤 원래 요청을 재실행한다.
- 모든 상태 변경 요청은 CSRF 쿠키와 `X-CSRF-Token` 헤더의 일치 여부 및 Origin을 검사한다.

### 6.2 개인 대시보드 — 사용 가능

- 참여 중인 다가오는 행사 최대 5건
- 7일 이내 마감되는 미완료 담당 업무 최대 8건
- 읽지 않은 수신 공지 최대 5건
- 관리 역할의 제출 검토 대기 수
- 이번 달·다음 달 통합 달력

프론트는 각 항목의 수치 카드와 상세 목록을 제공한다.

### 6.3 통합 달력과 개인 일정 — 사용 가능

달력 API는 최대 93일 범위에서 다음 정보를 합친다.

- 참여 또는 관리 가능한 행사·캠페인
- 본인에게 배정된 일반 업무 마감
- 제출 업무 마감
- 조 편성 업무 마감
- 승인된 조 활동 일정에서 생성된 개인 Reminder
- 사용자가 만든 개인 일정

개인 일정은 생성·수정·삭제할 수 있으며 허용된 색상만 저장한다. 행사, 업무 마감, 조 일정은 원본 도메인에서 생성되는 읽기 전용 달력 항목이다. 시간대 계산은 기본적으로 `Asia/Seoul`을 사용한다.

### 6.4 행사와 캠페인 — 부분 구현

백엔드:

- 행사·캠페인 생성
- 제목, 설명, 장소, 날짜, 시작·종료 시각, 부서, 담당자, 상태 저장
- 참여자 지정 및 교체
- 일반 임원은 참여 행사만 조회
- 부장단은 자기 부서 행사 또는 참여 행사 조회
- 회장단·교사는 현재 기수 행사 조회

프론트:

- 행사·캠페인 목록과 기본 정보 조회만 제공
- 행사 생성·상세·참여자 편집 화면은 아직 없음

### 6.5 업무와 칸반 보드 — 사용 가능

- 업무 유형: `SIMPLE`, `SUBMISSION`, `TEAM_FORMATION`
- 상태: `TODO`, `IN_PROGRESS`, `REVIEW`, `DONE`, `REJECTED`
- 업무 생성·수정
- 담당자 다중 지정
- 전체, 학년별 빠른 사용자 선택
- 마감일 지정
- 칸반 열별 상태 표시 및 상태 변경
- 일반 임원은 본인 담당 업무 조회
- 부장단은 본인이 생성했거나 자기 부서 구성원에게 배정된 업무 조회
- 회장단·교사는 현재 기수 업무 조회

제출 이력이 생긴 뒤에는 업무 유형을 바꿀 수 없다. 부장단은 자신이 만든 업무만 수정하고 검토할 수 있다.

### 6.6 내용·파일 제출과 승인·반려 — 사용 가능

- 담당자가 텍스트 또는 파일을 제출
- 재제출 시 `SubmissionVersion`을 증가시켜 버전 이력 보존
- 제출 시 업무 상태가 `REVIEW`로 변경
- 관리자가 최신 제출 버전, 제출자, 파일, 조 편성 내용을 검토
- 승인 시 업무 상태 `DONE`
- 반려 시 사유 필수, 업무 상태 `REJECTED`
- 파일은 메타데이터를 DB에 저장하고 실제 내용은 웹 루트 밖의 비공개 저장소에 저장
- 다운로드할 때 제출자, 담당자, 생성자, 회장단, 교사 여부를 검사한 뒤 스트리밍

파일 저장 키는 원본 이름 대신 UUID 기반 불투명 경로를 사용한다. 기본 파일당 최대 크기는 20MiB다. 실행 파일, 셸 스크립트, JavaScript, HTML, SVG 등 위험 형식은 확장자와 MIME Type 기준으로 차단한다.

### 6.7 조 편성 — 부분 구현

동작하는 흐름:

- 조 편성 업무 담당자가 여러 조를 작성
- 조 이름, 역할, 조장, 조원, 활동 일시, 설명 입력
- 한 사용자의 여러 조 중복 배정 방지
- 현재 기수의 활성 사용자만 조원으로 지정
- 관리자 승인 전에는 초안 상태
- 승인 시 조를 확정하고 각 조원의 개인 달력에 조 일정 Reminder 생성
- 행사와 연결된 조는 승인 시 조원을 행사 참여자로 자동 추가
- 관리자가 API로 확정 조를 직접 생성·수정 가능

현재 프론트는 업무 제출 안에서 조 편성을 작성하고 검토하는 흐름을 제공한다. 확정 조 전체 목록 및 독립적인 조 관리 화면은 없다.

### 6.8 공지 — 부분 구현

지원 유형:

- 일반 공지
- 행사 공지
- 참여 조사
- 선착순 공지

동작하는 흐름:

- 관리 역할의 공지 생성·수정
- 수신자 다중 지정과 학년별 빠른 선택
- 중요 공지 고정
- 수신 여부와 읽은 시각 저장
- 일반 임원은 수신 공지만 조회
- 부장단은 수신 공지와 본인이 작성한 공지 조회
- 선착순 신청 시 DB 행 잠금(`SELECT ... FOR UPDATE`)으로 정원 경쟁 처리
- 정원 초과 시 대기열 배정
- 확정 신청 취소 시 대기 순번이 가장 빠른 사용자를 자동 승격

프론트에는 공지 작성·수정·조회·선착순 신청 버튼이 연결되어 있다. 읽음 처리, 신청 취소, 현재 신청 상태와 대기 순번 표시는 아직 화면에 연결되지 않았다.

### 6.9 날짜 및 시간 변경 요청 — API 사용 가능

- 행사 참여자만 변경 요청 생성 가능
- 요청 날짜, 시작·종료 시각, 사유 저장
- 관리 역할이 승인 또는 반려
- 부장단은 자기 부서 행사 요청만 처리
- 반려 사유 필수

현재 프론트 화면은 없다. 또한 승인 결과가 행사 원본 날짜를 자동 변경하는 구조가 아니라 요청 상태만 갱신한다.

### 6.10 출석 — API 사용 가능

- 행사 참여자 기반 체크리스트 생성
- 상태: 대기, 출석, 결석, 지각, 조퇴
- 메모와 기록자 저장
- 다건 일괄 저장
- 전체 출석 처리
- 상태별 요약 집계
- 행사 당일 여부 및 행사 시간 중 여부 계산
- 부장단은 자기 부서 또는 본인 담당 행사 관리
- 일반 참여자는 본인 출석 정보만 조회
- 변경 전후 상태와 메모를 `AuditLog`에 JSON으로 기록

백엔드와 테스트는 구현되어 있으나 현재 프론트 출석 체크 화면은 없다.

### 6.11 사용자·부서·기수 관리 — 부분 구현

백엔드:

- 현재 기수의 활성 사용자 디렉터리와 학년 필터
- 역할별 사용자 목록 범위 제한
- 교사의 사용자 생성·수정
- 역할, 활성 상태, 부서, 학년, 비밀번호 관리
- 부서 조회·생성
- 기수 조회·생성
- 새 현재 기수를 만들 때 기존 기수의 `is_current` 해제

프론트:

- 교사의 사용자 목록 조회와 새 계정 생성
- 역할·학년·부서 지정
- 사용자 수정·비활성화, 부서 생성, 기수 생성 화면은 없음

### 6.12 개인 Reminder — 부분 구현

- 본인의 현재 기수 Reminder 조회
- 개인 Reminder 생성
- 개인 일정 색상 검증
- 조 편성 승인 시 시스템이 조원별 `TEAM` Reminder 생성
- 대시보드 달력에서 개인 Reminder를 생성·수정·삭제

독립적인 Reminder 목록 화면은 없지만 달력의 개인 일정 기능으로 핵심 CRUD를 사용할 수 있다.

### 6.13 PWA — 설치 뼈대

- `manifest.webmanifest`
- 독립 실행형 표시 모드
- 테마 색상과 SVG 앱 아이콘
- 서비스 워커 등록

서비스 워커는 설치·활성화만 처리한다. 정책에 맞게 오프라인 캐시, IndexedDB, Background Sync를 사용하지 않는다. `push` 이벤트 리스너는 비어 있으므로 실제 푸시 알림은 동작하지 않는다.

### 6.14 아직 준비되지 않은 기능

다음 기능은 관련 SQLAlchemy 모델과 Pydantic 스키마가 존재하거나 라우트가 등록되어 있지만, 호출하면 명시적으로 HTTP 501을 반환한다.

| 기능 | 현재 준비된 부분 | 빠진 부분 |
|---|---|---|
| 회의록·녹음 파일 | `MeetingRecord`, 참석자, 파일 연결 모델과 API 스키마 | CRUD, 업로드 연결, 화면 |
| 댓글 Thread | 대상 유형·대상 ID·부모 댓글 모델 | 조회·작성·수정·삭제, 화면 |
| 빠른 메모 | `QuickMemo` 모델과 스키마 | API 로직과 화면 |
| 저장 항목 | `SavedItem` 모델과 스키마 | API 로직과 화면 |
| 앱 내부 알림 | `Notification` 모델 | 생성·조회·읽음 처리와 화면 |
| PWA 푸시 | `PushSubscription` 모델, 서비스 워커 자리 | 구독 저장, VAPID, 발송 로직 |
| 기수 아카이브 | 라우트 계약 | 조회·잠금·복원 정책과 화면 |
| 감사 로그 조회 | 출석 로그 적재 | 교사용 조회 API와 화면 |

## 7. 주요 데이터 모델

### 조직과 인증

- `Department`: 부서
- `Term`: 기수와 운영 기간
- `User`: 사용자, 역할, 부서, 기수, 학년, 활성 상태, 세션 버전
- `RefreshSession`: Refresh Token 해시, 만료, 폐기 시각

### 행사와 출석

- `Event`: 행사·캠페인
- `EventParticipant`: 행사 참여자
- `Attendance`: 참여자의 출석 상태와 메모

### 업무와 제출

- `Task`: 업무
- `TaskAssignee`: 업무 담당자
- `Submission`: 제출 단위와 검토 상태
- `SubmissionVersion`: 재제출 버전
- `StoredFile`: 비공개 파일 메타데이터

### 공지와 일정 변경

- `Notice`: 공지
- `NoticeRecipient`: 수신자와 읽음 시각
- `NoticeApplication`: 선착순 신청 상태와 순번
- `ScheduleChangeRequest`: 행사 일정 변경 요청

### 조와 개인 도구

- `Team`, `TeamMember`: 조와 조원
- `Reminder`: 개인·조 일정
- `QuickMemo`, `SavedItem`: 빠른 메모와 저장 항목

### 후속 기능용 모델

- `MeetingRecord`, `MeetingAttendee`
- `Comment`
- `Notification`, `PushSubscription`
- `AuditLog`

## 8. API 설계

- 기본 경로: `/api/v1`
- 형식: JSON 기반 REST API
- 파일 업로드: `multipart/form-data`
- 파일 다운로드: 권한 검사 후 `FileResponse`
- 오류 형식: 상태 코드, 기계 판독용 `code`, 사용자용 `detail`
- 입력 검증: Pydantic
- OpenAPI: FastAPI 자동 생성
- DB I/O: `AsyncSession`

기능별 라우터는 인증, 달력, 관리, 대시보드, 행사, 업무, 공지, 조, 출석, 회의록, 댓글, 개인 도구, 알림, 아카이브, 감사 로그로 분리되어 있다.

## 9. 보안 구조

### 적용된 보호

- Argon2 계열 권장 설정으로 비밀번호 해시
- JWT HS256 서명
- Access/Refresh Token 유형 분리
- Refresh Token 원문 대신 SHA-256 Digest 저장
- HttpOnly 인증 쿠키
- `SameSite=Lax`
- 운영 환경에서 `COOKIE_SECURE=true` 사용 가능
- CSRF Double Submit 방식과 Origin 검사
- DB에서 사용자 활성 상태와 세션 버전 재검증
- 역할·부서·기수·담당 범위의 서버 측 검사
- 비공개 파일 경로와 다운로드 권한 검사
- 업로드 크기와 위험 파일 형식 제한
- 경로 탈출 방지

### 운영 시 필수 설정

- 기본 개발용 `JWT_SECRET`을 최소 32자 이상의 무작위 값으로 교체
- HTTPS 적용 및 `COOKIE_SECURE=true`
- Compose 예시의 PostgreSQL 개발용 비밀번호 교체
- 외부 공개 시 리버스 프록시 TLS 설정
- `.env`, DB 파일, 업로드 파일을 소스 배포물에서 분리

## 10. 배포 구조

Docker Compose는 세 서비스를 구성한다.

| 서비스 | 역할 |
|---|---|
| `postgres` | PostgreSQL 16 데이터베이스와 영구 볼륨 |
| `backend` | Alembic 적용 후 FastAPI/Uvicorn 실행 |
| `frontend` | React 정적 파일을 Nginx로 제공하고 `/api`를 백엔드로 프록시 |

프론트 이미지는 Node 22에서 빌드한 뒤 Nginx 이미지에 결과물만 복사하는 Multi-stage Build를 사용한다. 백엔드는 Python 3.12와 `uv.lock`을 기준으로 의존성을 고정한다. DB와 업로드는 각각 Docker Volume에 저장한다.

현재 파일 저장소는 비공개 로컬 볼륨이다. `FileStorage` 프로토콜과 `STORAGE_BACKEND` 설정에는 S3 교체 지점이 마련되어 있지만, `s3`를 선택하면 아직 Runtime Error가 발생한다. 따라서 “비공개 클라우드 오브젝트 스토리지” 요구사항은 후속 구현이 필요하다.

## 11. 테스트 및 품질 확인

2026-07-25 실행 결과:

| 검사 | 결과 |
|---|---|
| `ruff check app tests alembic` | 통과 |
| `pytest tests -q` | 16개 통과, 경고 1개 |
| `npm test` | 테스트 파일 2개, 테스트 3개 통과 |
| `npm run build` | TypeScript 및 Vite 프로덕션 빌드 통과 |

프론트 프로덕션 결과물:

- 화면별 HTML 문서: 10개, 각 약 0.52~0.53kB
- CSS: 8.73kB, gzip 2.42kB
- JavaScript: 224.12kB, gzip 68.88kB

백엔드 테스트는 인증 보호, CSRF, 비밀번호, JWT, 파일 저장, 업무 제출, 공지 수정, 출석, 조 승인과 달력 연계, 학년 필터, 미구현 API의 501 계약을 확인한다.

확인된 경고는 Starlette `TestClient`의 `httpx` 연동 방식에 대한 Deprecation Warning 1건이다. 현재 테스트 실패는 아니지만 향후 FastAPI/Starlette 테스트 의존성 갱신 시 정리할 필요가 있다.

## 12. 기술적 한계와 주의점

### 12.1 기능 완성도

- 데이터 모델이 존재한다고 기능이 완성된 것은 아니다. 여러 후속 라우트가 의도적으로 501을 반환한다.
- 백엔드에서 동작하지만 프론트 화면이 없는 기능이 많다. 특히 행사 관리, 출석, 독립 조 관리, 일정 변경 요청, 부서·기수 관리가 해당한다.
- PWA는 설치만 가능하고 푸시 발송은 동작하지 않는다.
- 저장소는 로컬 파일시스템이며 S3 구현은 없다.

### 12.2 코드에서 확인된 권한·집계 보완 후보

- `PUT /events/{event_id}/participants`는 행사 생성 API와 달리 새 참여자가 현재 기수의 활성 사용자인지 검증하지 않는다.
- 동일 참여자 교체 API는 회장단·교사 경로에서 대상 행사의 `term_id`가 현재 사용자 기수와 같은지 명시적으로 확인하지 않는다.
- 교사의 `PATCH /users/{user_id}`는 대상 사용자의 기수 범위를 확인하지 않는다.
- 대시보드 `review_count`는 현재 기수나 부장단의 생성 업무 범위를 적용하지 않고 전체 `SUBMITTED` 건수를 센다. 실제 검토 목록과 숫자가 달라질 수 있다.

위 항목은 현재 테스트 통과 여부와 별개로 다중 기수 데이터가 쌓이는 운영 환경 전에 보완하는 것이 안전하다.

### 12.3 유지보수성

- 주요 프론트 페이지가 하나의 `App.tsx`에 집중되어 있어 기능이 늘어나면 페이지·도메인 단위 분리가 필요하다.
- 백엔드 서비스 계층은 조·달력 연계에만 일부 적용되어 있다. 공지 선착순, 제출 검토 등 복잡한 로직도 서비스 계층으로 분리하면 테스트와 재사용성이 좋아진다.
- 현재 테스트 수는 MVP 핵심 계약 검증에는 유용하지만 브라우저 E2E, PostgreSQL 통합 테스트, 동시 선착순 신청, Refresh Token Rotation 전체 흐름은 추가 검증이 필요하다.

## 13. 권장 다음 작업

우선순위는 다음과 같이 잡는 것이 적절하다.

1. 행사 참여자 교체, 사용자 수정, 대시보드 검토 수의 기수·권한 범위 보완
2. 출석 체크리스트와 행사 관리 프론트 화면 연결
3. 공지 읽음·취소·대기 순번 및 일정 변경 요청 화면 연결
4. 부서·기수·사용자 수정 관리 화면 구현
5. 회의록과 녹음 파일 기능 구현
6. 댓글, 빠른 메모, 저장 항목, 내부 알림 구현
7. Web Push 구독·발송과 운영용 S3 호환 저장소 구현
8. PostgreSQL 통합 테스트와 주요 브라우저 E2E 테스트 추가

## 14. 결론

StudentFlow의 실제 애플리케이션 소스는 1MiB보다 작다. 현재 200MB를 크게 넘는 이유는 이미지가 아니라 **프론트엔드 의존성, Python 가상환경, Python 패키지 캐시가 함께 들어 있기 때문**이다. 제출용 또는 전달용 압축본에서는 재생성 가능한 개발 디렉터리를 제외하면 크기를 대폭 줄일 수 있다.

기능 측면에서는 인증, 대시보드, 업무·제출·검토, 공지, 조 편성·달력 연동의 MVP 흐름이 실제로 동작한다. 반면 회의록, 댓글, 알림, 아카이브 등은 아직 도메인 모델과 계약 위주이며, 출석과 일부 관리 기능은 백엔드는 구현됐지만 사용자 화면이 없다. 따라서 이 프로젝트는 “전체 기능 완성본”보다는 **핵심 운영 흐름이 작동하고 후속 기능의 데이터 구조와 API 경계를 마련한 MVP**로 평가하는 것이 정확하다.
